{
	"name": "@three-ws/node-operator",
	"version": "0.1.0",
	"description": "Operator client for the three.ws open inference network (roadmap phase 4). Registers a Solana-ed25519 node identity, polls the platform job queue, executes real local inference, and returns cryptographically signed results.",
	"license": "MIT",
	"type": "module",
	"private": true,
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"start": "node src/index.js",
		"register": "node src/index.js --register-only",
		"self-test": "node src/index.js --self-test",
		"e2e": "node scripts/e2e-local.mjs",
		"test": "vitest run"
	},
	"dependencies": {
		"@huggingface/transformers": "^4.2.0",
		"tweetnacl": "^1.0.3"
	},
	"devDependencies": {
		"vitest": "^4.1.9"
	}
}
