{
	"name": "home-relay",
	"version": "1.0.0",
	"private": true,
	"description": "Terminates the outbound WebSocket that a three.ws Home Assistant integration dials from inside a LAN-only house, and multiplexes allowlisted platform requests onto it.",
	"type": "module",
	"main": "src/index.js",
	"scripts": {
		"start": "node src/index.js",
		"build:allowlist": "node scripts/gen-allowlist.mjs"
	},
	"engines": {
		"node": ">=20"
	},
	"dependencies": {
		"ws": "^8.21.0"
	}
}
