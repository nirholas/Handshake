# Worker-level deps. MDM's own inference-path deps (CLIP, scipy, tqdm, smplx)
# are pip-installed directly in the Dockerfile (the cloned repo ships no
# requirements.txt of its own — see the Dockerfile comment).
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.0.0
numpy>=1.24.0
google-cloud-storage==2.18.0
httpx>=0.27.0
