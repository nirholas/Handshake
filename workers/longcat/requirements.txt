# Service dependencies. The LongCat model + avatar dependencies install
# separately from requirements-model.txt, under constraints.txt.
#
# huggingface-hub is deliberately NOT pinned here. It used to be, at ==0.30.2,
# for an in-process weights download that no longer exists (weights are staged
# once by stage-weights.sh on the operator's machine, never by the container).
# Because this file installs last, that pin silently downgraded the hub below
# diffusers 0.35.1's huggingface-hub>=0.34.0 floor, and pip reported the conflict
# as a non-fatal warning the build gates could not catch. Let diffusers and
# transformers resolve it, with the floor enforced in constraints.txt.
fastapi==0.115.12
uvicorn[standard]==0.34.2
httpx==0.28.1
google-cloud-firestore==2.20.1
google-cloud-storage==2.19.0
pydantic==2.11.3
