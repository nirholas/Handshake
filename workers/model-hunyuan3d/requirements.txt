# hy3dgen itself is installed in the Dockerfile from a pinned clone of
# Tencent/Hunyuan3D-2 (its texgen CUDA/pybind11 extensions must be compiled
# in-tree; a bare pip VCS install of the package subdirectory does not build
# them). Everything here is pinned or bounded: the 2026-07-16 model-trellis
# outage proved an unpinned transformers resolves a torch-2.5-era release
# (DTensor import) that fails to load on the torch pinned below.

# ── Model stack (torch 2.3.1/cu121 era, matching the Dockerfile pin) ─────────
diffusers==0.31.0
transformers==4.46.3
accelerate==0.34.2
safetensors>=0.4.0
einops==0.8.0
omegaconf==2.3.0
numpy==1.26.4

# ── Geometry / texture processing (hy3dgen shapegen + texgen runtime deps) ───
trimesh==4.5.3
pymeshlab==2023.12.post2
pygltflib==1.16.2
xatlas==0.0.9
opencv-python-headless==4.10.0.84
# hy3dgen's surface extractor does `from skimage import measure` at module
# import time (marching cubes), so scikit-image is a hard runtime dep even
# though the upstream requirements file comments it out.
scikit-image==0.24.0
# The in-tree extension builds run with --no-build-isolation (see Dockerfile),
# so their build-time imports must already be installed: differentiable_renderer
# imports pybind11 in setup.py; ninja parallelizes the CUDA compile.
pybind11==2.13.6
ninja==1.11.1.1

# ── Input preprocessing (hy3dgen.rembg background removal) ───────────────────
rembg==2.0.59
onnxruntime==1.20.1

# ── Image processing ──────────────────────────────────────────────────────────
Pillow>=10.3.0

# ── Server ────────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic>=2.0.0
httpx>=0.27.0

# ── GCP ───────────────────────────────────────────────────────────────────────
google-cloud-storage==2.18.0
