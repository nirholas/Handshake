        <!-- glance widget (solana-mobile/android-overlay) -->
        <receiver
            android:name=".glance.GlanceWidget"
            android:exported="true"
            android:label="@string/glance_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/glance_widget_info" />
        </receiver>

        <activity
            android:name=".glance.GlanceLinkActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="threews" android:host="glance" />
            </intent-filter>
        </activity>

        <!-- three.ws Drive (solana-mobile/android-overlay): Android Auto.
             The IOT category is the accurate one: a three.ws agent takes real
             actions on the user's connected devices through the home tools wired
             into /api/chat, with a safety gate that freezes anything physical
             until a person approves it. Docs: docs/carplay.md. -->
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/drive_automotive_app_desc" />

        <service
            android:name=".drive.DriveCarAppService"
            android:exported="true"
            android:label="@string/drive_car_title">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.IOT" />
            </intent-filter>
        </service>

        <service
            android:name=".drive.DriveWebService"
            android:exported="false"
            android:foregroundServiceType="microphone|mediaPlayback" />
