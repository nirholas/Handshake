# Cloudflare Pages — static deploy of the docs/ contact-sheet site.
# Used by the "Deploy to Cloudflare" button and `wrangler pages deploy docs`.
name = "robinhood-chain-examples"
pages_build_output_dir = "docs"
