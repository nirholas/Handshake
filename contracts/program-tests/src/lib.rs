//! Shared helpers for the Solana program invariant tests in `tests/`.
//!
//! The crate itself ships no runtime code: every contract under test is real
//! compiled SBF bytecode loaded into LiteSVM by the integration tests.
